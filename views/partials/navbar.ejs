<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@400;500;600;700&display=swap"
    rel="stylesheet">
  <title>Document</title>
</head>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Playfair+Display:wght@400;500;600;700&family=Segoe+UI:wght@400;500;600;700&display=swap');
  @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@500;600;700&display=swap');

  .glass-navbar {
    width: 100%;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 32px;
    background: transparent;
    backdrop-filter: blur(8px);
    /* optional soft blur */
    -webkit-backdrop-filter: blur(14px);
    /* border-bottom: 0.5px solid rgba(100,100,100,0.14); */
    font-family: 'Poppins', 'Playfair Display', 'Segoe UI', 'Cormorant Garamond', sans-serif;
    box-sizing: border-box;
  }

  .navbar-brand {
    font-family: 'Playfair Display', 'Poppins', serif;
    font-weight: 700;
    font-size: 2rem;
    color: #181818;
    letter-spacing: 0.03em;
    padding-right: 32px;
  }

  .navbar-links {
    display: flex;
    margin-right: 150px;
    gap: 3rem;
  }

  .nav-link {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.2rem;
    text-decoration: none;
    color: #232323;
    font-weight: 500;
    position: relative;
    transition: color 0.3s ease;
  }

  .nav-link::after {
    content: "";
    display: block;
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 1.5px;
    background: #f96c2b;
    /* Match your hover color */
    /* border-radius: 2px; */
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.25s cubic-bezier(.51, .01, .05, 1);
  }

  .nav-link:hover {
    color: #f96c2b;
  }

  .nav-link:hover::after {
    transform: scaleX(1);
  }

  .navbar-toggle {
    display: none;
    font-size: 2.2rem;
    background: none;
    border: none;
    color: #181818;
    cursor: pointer;
  }
.collections-container {
  position: relative;
}

.collections-dropdown {
  position: absolute;
  top: 56px; /* Align with navbar */
  left: 0;
  background: #fff;
  box-shadow: 0 10px 27px rgb(255 138 89 / 15%);
  border-radius: 16px;
  display: none;
  min-width: 160px;
  z-index: 200;
  padding: 10px 0;
  font-family: 'Poppins', sans-serif;
}

.collections-container.active .collections-dropdown {
  display: block;
}

/* Reuse .dropdown-link styles if you want */

  .profile-container {
    position: relative;
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    color: #232323;
    font-family: 'Cormorant Garamond', serif;
    user-select: none;
    /* height: 60px; */
  }

  .profile-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    object-fit: cover;
    border: 1.5px solid #f96c2b;
    transition: box-shadow 0.25s;
    align-self: center;
  }

  .profile-username {
    font-size: 1.2rem;
    color: #232323;
    position: relative;
    display: inline-block;
    padding-bottom: 7px;
    /* Gap for underline, now matches nav-link */
    line-height: 1.2;
    transition: color 0.3s;
  }

  .profile-username::after {
    content: "";
    display: block;
    position: absolute;
    left: 0;
    bottom: -3px;
    /* Slightly negative for extra gap */
    width: 100%;
    height: 1.5px;
    background: #f96c2b;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.25s cubic-bezier(.51, .01, .05, 1);
  }

  .profile-container:hover .profile-username {
    color: #f96c2b;
  }

  .profile-container:hover .profile-username::after {
    transform: scaleX(1);
  }

  .profile-dropdown {
    position: absolute;
    top: 56px;
    right: 0;
    background: #fff;
    box-shadow: 0 10px 27px rgb(255 138 89 / 15%);
    border-radius: 16px;
    display: none;
    min-width: 220px;
    z-index: 200;
    padding: 14px 0;
    font-family: 'Cormorant Garamond', serif;
  }

  .profile-container.active .profile-dropdown {
    display: block;
  }

  /* Dropdown link: icon left, text right, with underline on text */
  .dropdown-link {
    display: flex;
    align-items: center;
    padding: 10px 24px;
    font-size: 1rem;
    color: #232323;
    background: none;
    border: none;
    cursor: pointer;
    width: 100%;
    text-align: left;
    gap: 16px;
    transition: color 0.2s;
  }

  .dropdown-icon {
    width: 25px;
    height: 25px;
    flex-shrink: 0;
    color: #000000;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .dropdown-icon svg path,
  .dropdown-icon svg circle,
  .dropdown-icon svg rect {
    stroke: #ababab;
    transition: stroke 0.2s;
  }

  .dropdown-link:hover .dropdown-icon svg path,
  .dropdown-link:hover .dropdown-icon svg circle,
  .dropdown-link:hover .dropdown-icon svg rect {
    stroke: #f96c2b;
  }

  .dropdown-text {
    position: relative;
    padding-bottom: 5px;
    transition: color 0.3s;
    color: inherit;
    font-size: 1.1rem;
    font-family: 'Cormorant Garamond', serif;
  }

  .dropdown-text::after {
    content: "";
    display: block;
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 2px;
    background: #f96c2b;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.25s cubic-bezier(.51, .01, .05, 1);
  }

  .dropdown-link,
  .dropdown-link .dropdown-text,
  .profile-dropdown a {
    text-decoration: none !important;
  }

  .dropdown-link:hover .dropdown-text {
    color: #f96c2b;
  }

  .dropdown-link:hover .dropdown-text::after {
    transform: scaleX(1);
  }

  .dropdown-link:hover {
    background: none;
  }

  /* Remove old .dropdown-item hover effects */
  .profile-dropdown .dropdown-link {
    background: none;
    border-radius: 0;
  }


  @media (max-width: 820px) {
    .glass-navbar {
      padding: 0 16px;
    }

    .navbar-brand {
      font-size: 1.4rem;
      padding-right: 16px;
    }

    .nav-link {
      font-size: 1rem;
    }
  }

  @media (max-width: 700px) {
    .navbar-links {
      display: none;
    }

    .navbar-toggle {
      display: block;
    }
  }

  /* Mobile Slide Nav */
  .mobile-nav {
    display: none;
    position: fixed;
    top: 0;
    right: -320px;
    width: 260px;
    height: 100vh;
    background: rgba(255, 255, 255, 0.46);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    box-shadow: -4px 0 18px rgba(0, 0, 0, 0.09);
    z-index: 102;
    transition: right 0.30s cubic-bezier(.27, .19, .56, .81);
  }

  .mobile-nav.open {
    display: block;
    right: 0;
  }

  .mobile-nav-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 60px;
    border-bottom: 1px solid rgba(100, 100, 100, 0.13);
    padding: 0 22px;
  }

  .mobile-nav-links {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 28px 22px;
  }

  .close-icon {
    font-size: 2rem;
    color: #181818;
    cursor: pointer;
  }

  .mobile-nav-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(20, 41, 71, 0.14);
    z-index: 101;
  }

  .mobile-nav.open~.mobile-nav-overlay {
    display: block;
  }

  /* Mobile Styles for Profile Dropdown */
  @media (max-width: 820px) {
    .profile-container {
      height: 50px;
    }

    .profile-username {
      font-size: 1.05rem;
    }

    .profile-dropdown {
      min-width: 150px;
      top: 47px;
    }

    .dropdown-link {
      font-size: 0.97rem;
      padding: 8px 14px;
    }

    .dropdown-icon {
      width: 18px;
      height: 18px;
    }
  }
</style>

<body>
  <!-- Navbar Start -->
  <nav class="glass-navbar">
    <div class="navbar-brand">FlexBase</div>
    <div class="navbar-links" id="navbarLinks">
      <a href="/" class="nav-link">Home</a>
      <a href="/explore" class="nav-link">Explore</a>
      <a href="/create" class="nav-link">Create</a>
      <a href="/chat" class="nav-link">Chat</a>
      <div class="collections-container">
        <a href="#" class="nav-link collections-link" id="collectionsLink">Collections</a>
        <div class="collections-dropdown" id="collectionsDropdown">
          <a href="/collections/add" class="dropdown-link">
            <span class="dropdown-text">Add Shoe</span>
          </a>
          <a href="/collections" class="dropdown-link">
            <span class="dropdown-text">View Collection</span>
          </a>
        </div>
      </div>
      <% if (typeof user !=="undefined" && user) { %>
        <div class="profile-container">
          <img src="<%= user.profileImage || '/images/default-profile.jpg' %>" alt="Profile" class="profile-avatar"
            id="profileAvatar" />
          <span class="profile-username" id="profileUsername">
            <%= user.username %>
          </span>
          <div class="profile-dropdown" id="profileDropdown">
            <a href="/profile" class="dropdown-link">
              <span class="dropdown-icon">
                <!-- User SVG -->
                <svg width="42" height="42" viewBox="0 0 20 20" fill="none">
                  <circle cx="10" cy="6.5" r="3.5" fill="#b4b4b4" />
                  <path d="M3 17c0-2.5 3.5-4.5 7-4.5s7 2 7 4.5" stroke="#b4b4b4" stroke-width="2" fill="none" />
                </svg>
              </span>
              <span class="dropdown-text">View Profile</span>
            </a>
            <a href="/logout" class="dropdown-link">
              <span class="dropdown-icon">
                <!-- Signout SVG -->
                <svg width="42" height="42" viewBox="0 0 20 20" fill="none">
                  <path d="M15 10H7M12 7l3 3-3 3" stroke="#b4b4b4" stroke-width="2" stroke-linecap="round"
                    stroke-linejoin="round" />
                  <rect x="3" y="3" width="14" height="14" rx="7" stroke="#b4b4b4" stroke-width="2" fill="none" />
                </svg>
              </span>
              <span class="dropdown-text">Logout</span>
            </a>
          </div>


        </div>
        <% } else { %>
          <a href="/login" class="nav-link">Login/Signup</a>
          <% } %>

    </div>
    <div class="navbar-toggle" id="navbarToggle">
      &#9776;
    </div>
  </nav>
  <!-- Overlay for slide menu -->
  <div class="mobile-nav-overlay" id="mobileNavOverlay"></div>
  <!-- Slide menu for mobile -->
  <div class="mobile-nav" id="mobileNav">
    <div class="mobile-nav-header">
      <span class="navbar-brand">FlexBase</span>
      <span class="close-icon" id="closeNav">&#10005;</span>
    </div>
    <div class="mobile-nav-links">
      <a href="/" class="nav-link">Home</a>
      <a href="/explore" class="nav-link">Explore</a>
      <a href="/create" class="nav-link">Create</a>
      <a href="/chat" class="nav-link">Chat</a>
      <a href="/collections" class="nav-link">Collections</a>
      <% if (typeof user !=="undefined" && user) { %>
        <div class="profile-container">
          <img src="<%= user.profileImage || '/images/default-profile.png' %>" alt="Profile" class="profile-avatar"
            id="profileAvatar" />
          <span class="profile-username" id="profileUsername">
            <%= user.username %>
          </span>
          <div class="profile-dropdown" id="profileDropdown">
            <a href="/profile" class="dropdown-item">View Profile</a>
            <a href="/logout" class="dropdown-item">Logout</a>
          </div>
        </div>
        <% } else { %>
          <a href="/login" class="nav-link">Login/Signup</a>
          <% } %>
    </div>
  </div>
  <script src="/js/navbar.js"></script>
</body>

</html>